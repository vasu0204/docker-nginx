# Use a more secure, lightweight, and maintained Nginx base image
FROM bitnami/nginx:latest

# Copy the HTML file into the Nginx server directory
COPY index.html /opt/bitnami/nginx/html/

# Expose the web server port
EXPOSE 8080

# Use the default CMD provided by the Bitnami image
